# Mission Control — Releases

Public auto-update channel for the Mission Control desktop app (Helionix Labs).
Installers only — no source. The app updates itself from the latest release here.

Latest: see the Releases tab. Run `MissionControl-<version>-x64.exe`.
